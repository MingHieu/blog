{
  "name": "mh-lab",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:local": "astro build && node scripts/generate-og-screenshots.js",
    "check:og": "node scripts/check-og-required.js",
    "prepare": "git config core.hooksPath .githooks && chmod +x .githooks/*",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.3.2"
  }
}